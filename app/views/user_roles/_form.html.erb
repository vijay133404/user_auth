<div class="container-fluid">
<%= form_for(user_role ,:class=>"form-horizontal",:remote=>true) do |f| %>

  <% if user_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_role.errors.count, "error") %> prohibited this user_role from being saved:</h2>

      <ul>
      <% user_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :role_name,:class=>"col-sm-3 control-label" %>
      <div class="col-md-3  inputGroupContainer">
      <%= f.text_field :role_name,:class=>"form-control" %>
    </div>
  <br><br>
     <div class="form-group">

      <%= f.submit :class=>"btn btn-success" %>
    </div>
    </div>
  <% end %>

</div>



