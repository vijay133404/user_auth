  <div id="new_update">
  <%= form_for(city,:class=>"form-horizontal",:remote=>true) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <div class="form-group">

        <%= f.label :city_name,:class=>"col-sm-2 control-label" %>
        <div class="col-md-3  inputGroupContainer">
        <%= f.text_field :city_name,:class=>"form-control" %>
      </div>
      </div>
       

       <div class="form-group">
        <%= f.label :StateName,:class=>"col-sm-1 control-label" %>
        <div class="col-md-3  inputGroupContainer">
        <%= f.select(:state_id, State.all.collect {|u| [u.state_name, u.id]},{},:class=>"form-control",:prompt => 'Select State') %>
      </div>
      </div>    
    <br><br><br><br>


      <div class="form-group">
      <div class="col-md-10 inputGroupContainer">
        <%= f.submit :class=>"btn btn-success" %>
      </div>
  </div>
<% end %>

</div>